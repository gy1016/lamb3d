/**
 * Shader data grouping.
 */
export declare enum ShaderDataGroup {
    /** Scene group. */
    Scene = 0,
    /** Camera group. */
    Camera = 1,
    /** Renderer group. */
    Renderer = 2,
    /** material group. */
    Material = 3
}
